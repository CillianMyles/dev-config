# follow flutter instructions to fix toolchain issues
flutter doctor -v

# make sure desired ruby version installed e.g. rbenv
rbenv install 3.2.1
rbenv global 3.2.1

# install cocoapods
gem install cocoapods --version 1.12.0
